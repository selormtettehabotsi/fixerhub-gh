# Java
target/
*.class
.mvn/

# Node / Expo
node_modules/
.expo/
dist/

# Environment variables — NEVER commit
.env
.env.local
.env.*
frontend/.env
frontend/.env.local
frontend/.env.*

# Firebase — service account + Android app config (both are credentials)
**/firebase-service-account.json
**/google-services.json
frontend/google-services.json

# Built app binaries — never commit (large, and rebuildable)
*.apk
*.aab


# IDEs
.idea/
.vscode/
*.iml