server:
  port: 8087

spring:
  application:
    name: admin-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: ${JWT_SECRET:fixerhub-secret-key-must-be-at-least-256-bits-long}
