# D1: superseded — this service is built by the parameterized backend/Dockerfile.
# docker compose --profile app up -d --build   (see docker-compose.yml)
#
# Kept only so old scripts fail loudly with this message instead of a broken build.
FROM eclipse-temurin:21-jre-alpine
RUN echo "Use backend/Dockerfile with build-arg MODULE=review-service (via docker compose --profile app)" && exit 1
